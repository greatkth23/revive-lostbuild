# 다음 작업자 인수인계 메모

갱신일: 2026-08-12

## 현재 상태

- 계산기 버전: `2.7.2`
- 파서 버전: `lostark-api-v2.7.1`
- 기본 계산 규칙: `current-v2.7.2`
- 지식 DB: `weather-artist-v0.4`
- 검증: `python -m unittest -v` 기준 20개 테스트 통과

## 완료한 핵심 작업

- `ArkGrid.Effects[]`의 공격력·추가 피해·보스 피해를 계산 카테고리에 연결했다.
- 활성 젬 개별 합계와 `ArkGrid.Effects[]`는 같은 효과 레벨의 중복 표현으로
  판단했다. `Effects[]`를 권위값으로 사용하며 개별 젬 값은 검산/대체용이다.
- 카르마 깨달음은 레벨당 무기 공격력 `0.1%`로 적용한다.
- 프로필 공격력과 재구성 공격력이 다르면 재구성 과정은 보고하되 실제 스킬
  피해에는 프로필 공격력을 사용한다.
- 아크그리드 질서/혼돈 코어의 포인트, 유물/고대 분기와 합연산·곱연산 규칙을
  구현했다. 18~20P는 활성 포인트마다 독립 적용한다.
- `공간 가르기`를 우산 스킬로 수정했고 봄날꽃씨·시아나즈 보고서를 갱신했다.
- 봄날꽃씨의 `바람송곳`, `칼바람`, `몰아치기`, `회오리 걸음` 모션식을
  추가하고 개별 계산 보고서를 생성했다.
- 제공된 기본 타격 모션계수·모션상수에는 트라이포드 피해 배율이 없다.
  선택 트라이포드의 직접 피해, 추가 공격, 총 증가 피해, 치명타 피해를 공식
  API 툴팁에서 별도로 파싱하되, 트라이포드 자체가 만든 별도 타격은 출처를
  연결하여 중복 곱연산을 막는다.
- 예외적으로 몰아치기 3타는 `(1타+2타)×94.8%`와 일치하는 `공간베기`
  추가 타격이다. 3타 모션식은 합산하되 공간베기 `×1.948`을 다시 곱하지 않는다.
- `current-v2.7.2`는 사용자 요청에 따른 비교 규칙으로, 프로필 공격력과
  재구성 공격력이 달라도 스킬 피해에는 재구성 공격력을 사용한다.
- 일반 피해 보석은 해당 스킬 계산에 한 번만 적용하며 재사용 대기시간 보석은
  1회 피해가 아닌 향후 로테이션/DPS용 데이터로 보존한다.
- 계산 보고서에서 `핵심 원본 데이터 발췌`와 `아크그리드 전체 효과 적용 전후`
  섹션은 출력하지 않는다. 원본/파싱 자료는 `outputs`의 JSON에서 확인한다.

## 꼭 확인할 계산 가정

- 트라이포드 피해 증가는 기본 모션식과 각각 독립 곱연산한다.
- `큰 센바람`, `공간베기`의 추가 공격은 별도 모션식이 없으므로 툴팁의 추가
  피해율을 스킬 전체 배율로 적용한다.
- `칼바람-벼락` 치명타 피해 `+210%`, `몰아치기-우레` 치명타 피해 `+180%`는
  API 툴팁에서 파싱해 공용 치명타 피해에 합산한다.
- `역류`는 기류 보호막을 보유한 최대 유리 조건에서 적용한다.
- 네 스킬의 방향 타입은 API에 명시 필드가 없어 `NON_DIRECTIONAL`로 잠정
  분류했다.
- 보고서는 `max-favorable-example-boss-v1` 시나리오다. 운명: 비연참 및
  기류 보호막 조건이 충족된 최대 유리 조건을 사용한다.

## 봄날꽃씨 v2.7.2 결과

`outputs/봄날꽃씨_우산스킬_추정보고서_v2.7.2.md`에 공간 가르기,
바람송곳, 칼바람, 몰아치기, 회오리 걸음, 우레바람 결과와 트라이포드별
적용 배율을 통합했다. 입력 스냅샷 시각은
`2026-08-12T15:57:51.515373+09:00`이다.

## 주요 경로와 재현 명령

- 계산기: `work/lostark_damage_test.py`
- 테스트: `work/test_lostark_damage_test.py`,
  `work/test_damage_knowledge_db.py`
- DB 시드: `work/db/weather-artist-v0.4_seed.json`
- 변경 이력: `work/DAMAGE_CALCULATOR_CHANGELOG.md`
- 사용법: `work/DAMAGE_CALCULATOR_README.md`
- 결과 보고서와 API/파싱 JSON: `outputs/`

```powershell
cd api-chatgpt-conversation-6a6309ab-7de4-8342\work
python -m unittest -v
```

추가 스킬을 지원할 때는 해당 캐릭터의 선택 트라이포드 툴팁 문구가
`parse_tripod_damage_effects()`의 분류 패턴으로 모두 구조화되는지 회귀 테스트를
먼저 추가한다.
